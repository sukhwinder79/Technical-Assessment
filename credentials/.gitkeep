# Put Google credentials here (both are gitignored):
#   service_account.json   — service-account key  (GOOGLE_AUTH_MODE=service_account)
#   oauth_client.json      — OAuth desktop client (GOOGLE_AUTH_MODE=oauth)
#   token.json             — written automatically after the first OAuth login
